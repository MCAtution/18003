#include<iostream>
    void main()
    {
        struct stk

        {
            int items[10];
            int top;

        }*s;
            int val;
              int f,r;
                f=-1;
                r=-1;
        s->top=-1;
        void Insert()
            {

            if (r==items[r]-1)
            cout<<"Queue Overflow"<<endl;
            else
                {
            if (f == - 1)
        f = 0;
        cout<<"Insert the element in queue : "<<endl;
        cin>>val;
        r++;
        queue[r] = val;
                }
            }
        }
