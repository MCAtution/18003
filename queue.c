#include<stdio.h>
    void main()
    {
        struct stk

        {
            int items[10];
            int top;

        }*s;
        s->top=-1;
        INSERT(struct stk *q,int x)
        {
            int isFull(struct stack x_stack)
            {
            if (a_stack.top < s->items[10] )
            return FALSE;
            else
            return TRUE;
            }
        }
    }
